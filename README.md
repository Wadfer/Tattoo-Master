В данном репозитории находится весь код программы, разделенный на несколько частей для того, чтобы было проще ориентироваться в коде. В папке `dist` лежит скомпилированная программа (`main.exe`). База данных создаётся автоматически (`tattoo_db.sqlite`) и хранит информацию для ИС тату-мастера.
